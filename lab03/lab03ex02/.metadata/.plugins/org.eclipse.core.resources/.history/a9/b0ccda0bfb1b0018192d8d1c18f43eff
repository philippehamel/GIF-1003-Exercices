/*
 * fonctions.cpp
 *
 *  Created on: 2018-02-27
 *      Author: etudiant
 */

#include "fonctions.h"
#include <iostream>

using namespace std;

void saisir_matrice(int p_matA[][], int p_matB[][], int* ligne, int* colonne) {
	cout << "Combien de lignes ont les matrices?" << endl;
	do{
		cout<<"Le nombre de lignes doit etre entre 2 et "<<MAX_CASES<<endl;
		cin >> *ligne;
	}while(*ligne < 2 || *ligne > MAX_CASES);
	cout << "Combien de rangees ont les matrices?" << endl;
	do{
		cout<<"Le nombre de colonnes doit etre entre 2 et "<<MAX_CASES<<endl;
		cin>>*colonne;
	}while(colonne < 2 || *colonne > MAX_CASES);


}

void somme(int p_matA[][], int p_matB[][], int p_matC[][], int ligne, int colonne) {
}

void affiche(int p_matC[][], int ligne, int colonne) {
}
